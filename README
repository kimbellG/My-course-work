Курсовая работа на языке С++. Тема работы учет клиентов и тарифов оператора сотовой компании.

Для поддержки русского языка в консоли используется кодировка ср1251.

В папке Исполняемый файл прикреплены димамические библиотеки, которые необходимы для работы программы.
Поместить их в одну папку и c исполняемым файлом.

В случае повторной компиляции присутствуют зависимости:
    1. OpenSSL, а точнее статическая библиотека -lcrypto. Прописать при компоновки include дирректории библиотеки.
    2. Не зависимость, но, при компиляции, все файлы поместить в один каталог.
